{
  "name": "moneybook",
  "version": "1.1.5",
  "description": "money book backend server",
  "main": "src/main.ts",
  "scripts": {
    "init": "npx -y pnpm install",
    "update": "npx -y pnpm update",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "start": "ts-node src/main -d mysql://root:root@nas.internal:3306/moneybook -a 127.0.0.1 -p 8000",
    "build": "node build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "commander": "^14.0.2",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.10",
    "@vercel/ncc": "^0.38.4",
    "pnpm": "^10.28.2",
    "prisma": "^7.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
