{
  "name": "moneybook",
  "version": "1.1.3",
  "description": "money book backend server",
  "main": "main.ts",
  "scripts": {
    "init": "npx -y pnpm install",
    "update": "npx -y pnpm update",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "start": "ts-node main -d mysql://root:root@localhost:3306/moneybook -a 0.0.0.0 -p 8000",
    "build": "node build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.0.1",
    "@prisma/client": "^7.0.1",
    "commander": "^14.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.1",
    "@vercel/ncc": "^0.38.4",
    "pnpm": "^10.23.0",
    "prisma": "^7.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
